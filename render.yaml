services:
  - type: web
    name: my-application
    runtime: docker
    region: oregon
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: SESSION
        value: "RGNK~T5qSaAEx"
      - key: DATABASE_URL
        fromDatabase:
          name: my-database
          property: connectionString
